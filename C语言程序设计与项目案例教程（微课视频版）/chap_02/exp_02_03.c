/*
*   定义变量a、变量b为整型，并输出其值
* 
*   样例输出：   23
*/

#include <stdio.h>

int main(void)
{
    int a, b = 3;               /* 定义整型变量 a, b, 并给变量 b 赋初值 3 */
    a = 2;
    printf("%d%d", a, b);

    return 0;
}